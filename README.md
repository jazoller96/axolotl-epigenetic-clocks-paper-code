# Project-specific code for “Axolotl epigenetic clocks offer insights into the nature of negligible senescence”

## Corresponding author

Maximina Yun  
max.yun@cimrbj.ac.cn

## Associated manuscript

Title: “Axolotl epigenetic clocks offer insights into the nature of negligible senescence”  
Journal: Nature Aging  
Manuscript ID: NATAGING-A06888C

## Overview

This repository contains project-specific code used to generate the analyses, models, validation results, figures, tables, and supplementary results reported in the manuscript “Axolotl epigenetic clocks offer insights into the nature of negligible senescence.”

The repository includes:

- `Rprojectcode_JZ/`: Project-specific R scripts for data processing, cohort/subset definition, model fitting, validation, and figure/table generation.
- `Rprojectcode_JZ_run_order.COMMAND`: A command file specifying the intended execution order of the scripts in `Rprojectcode_JZ/`.
- `Rprojectcode_Yullia/`: Code contributed by Yuliia Haluza for [BRIEF DESCRIPTION OF YULIIA’S CODE].

These scripts call reusable functions from the following R package:

**MammalMethylClock**  
https://github.com/jazoller96/mammalian-methyl-clocks/tree/main

The `MammalMethylClock` package contains wrapper functions and standard pipeline functions used by the manuscript-specific scripts in this repository.

## Required content checklist

This repository provides:

- Source code for the manuscript-specific analyses.
- A README file describing system requirements, installation, demo information, instructions for use, and reproduction instructions.
- A command file, `Rprojectcode_JZ_run_order.COMMAND`, specifying the order in which the project-specific R scripts in `Rprojectcode_JZ/` should be run.
- A license for code reuse.

A small demo dataset is not included because the manuscript-specific analyses require large methylation data files and associated sample annotation files described below and in the manuscript’s Data Availability Statement.

## System requirements

### Operating system tested

The project-specific code was run on:

- macOS Tahoe 26.4.1
- MacBook Pro 14-inch, 2023
- Apple M3 Max chip
- 64 GB memory

### R version tested

The project-specific code was run using:

- R version 4.5.2

### Required software

- R version 4.5.2 or a compatible version.
- The `MammalMethylClock` R package:
  https://github.com/jazoller96/mammalian-methyl-clocks/tree/main

### R package dependencies

The project-specific scripts call functions from `MammalMethylClock` and use additional R packages.

Major required R packages include:

- `tidyverse`
- `glmnet`
- `WGCNA` (from BioConductor)

Additional package calls may appear within individual R scripts.

### Non-standard hardware

No non-standard hardware is required.

The full manuscript analyses were run on a 2023 14-inch MacBook Pro with Apple M3 Max and 64 GB memory. Some analyses, especially model fitting and cross-validation steps, may require substantial memory and runtime depending on the size of the input methylation data and the number of samples analyzed.

## Installation guide

1. Install R.

2. Install the `MammalMethylClock` R package from GitHub (instructions are listed on the GitHub page, as this repo currently is NOT formatted correctly to be able to run `remotes::install_github("jazoller96/mammalian-methyl-clocks")`)

3. Install additional R package dependencies used by the project-specific scripts.

4. Download or clone this repository: https://github.com/jazoller96/axolotl-epigenetic-clocks-paper-code/tree/main

5. Place the required input data files in the expected local file structure described in the code.

  Typical installation time on a normal desktop computer: Approximately 10–30 minutes, depending on whether R package dependencies are already installed.

## Demo

A small demo dataset is not included because the manuscript-specific analyses require the manuscript-associated methylation data files and sample annotation files described in the Data Availability Statement.

The intended use of this repository is to document and run the manuscript-specific analysis workflow using the required manuscript-associated input data. The expected outputs are the manuscript figures, tables, validation results, model outputs, and supplementary results generated by the scripts listed in `Rprojectcode_JZ_run_order.COMMAND`.

## Instructions for use

### Input data

The project-specific scripts require manuscript-associated input data files described in the manuscript’s Data Availability Statement.

At least one script uses the following input files:

- `Axolotl/SampleSheetAgeN131final.csv`
- `Axolotl/all_probes_sesame_normalized.Rdata`
- `HumanAndArrayAnnotations/AllNormalizedDataCBUAndInfo.RData`
- `Frog/SampleSheetAgeN140final.csv`
- `Frog/all_probes_sesame_normalized.Rdata`

Expected local input file structure (within workspace):

<Workspace>/
  AllNormalizedDataCBUAndInfo.RData
  N131.ET0087.SalamanderMaxYun/
    SampleSheetAgeN131final.csv
    all_probes_sesame_normalized.Rdata (Axolotl)
  N140.2021-9212FrogChristofNiehrs/
    SampleSheetAgeN140final.csv
    all_probes_sesame_normalized.Rdata (Frog)

Input data location: In `Data` Folder next to this README file.

### Running the manuscript-specific workflow

The intended execution order for the scripts in `Rprojectcode_JZ/` is specified in:

`Rprojectcode_JZ_run_order.COMMAND`

The scripts should be run in the order listed in `Rprojectcode_JZ_run_order.COMMAND`.

The scripts perform manuscript-specific analysis steps, including:

- Data loading and processing.
- Cohort and treatment/control subset definitions.
- Model specification.
- Model fitting.
- Elastic-net regression and cross-validation.
- Validation analyses.
- Figure generation.
- Table generation.
- Supplementary result generation.

Random seeds used for elastic-net regression, cross-validation, and other stochastic procedures are set within the relevant R scripts.

### Expected output

The full workflow generates the reported manuscript outputs using the script titled: `Subset_AxolotlN131_PredictionResults.R`.  Not all figures are used for the final paper.  All figures used or partially used in Main, Supplement, or External Data are marked with a prefix.

Expected runtime for the full manuscript workflow: Runtime varies by analysis step and input data size. Model fitting and cross-validation steps are expected to require the longest runtime.

## Reproduction instructions

To reproduce the quantitative results reported in the manuscript:

1. Install R version 4.5.2 or a compatible version.
2. Install the `MammalMethylClock` R package from:
   https://github.com/jazoller96/mammalian-methyl-clocks/tree/main
3. Install the additional required R package dependencies listed above and/or called by the individual scripts.
4. Obtain the manuscript-associated input data described in the Data Availability Statement.
5. Place the input data in the expected local file structure described above.
6. Run the scripts in `Rprojectcode_JZ/` in the order specified in `Rprojectcode_JZ_run_order.COMMAND`.
7. Run scripts in `Rprojectcode_Yullia/`.
8. Compare the generated outputs with the reported manuscript figures, tables, validation analyses, model outputs, and supplementary results.

Some local file paths may need to be adjusted for use on another computer. These path changes should not alter the analysis order, model specifications, random seeds, or input data definitions.

## Code functionality description

A complete description of the code’s functionality is provided in the manuscript:

- Methods section
- Supplementary Methods

The manuscript Methods section describes the analysis pipeline, model fitting procedures, validation analyses, and figure/table generation at the level needed to understand the functionality of the code.

## License

The code in this repository is released under the MIT License. See the `LICENSE` file for details.

## Repository and archival DOI

GitHub repository:

https://github.com/jazoller96/axolotl-epigenetic-clocks-paper-code/tree/main

Archived release:

[INSERT ZENODO DOI]
